name: Test custom options - hide passed tests and coverage
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_REF: refs/heads/main
      GITHUB_EVENT_NAME: push
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        continue-on-error: true
        with:
          path: /tmp/nonexistent.xcresult
          token: ''
          title: 'Custom Test Results'
          show-passed-tests: 'false'
          show-code-coverage: 'false'
          upload-bundles: 'never'
      - name: Assert action failed gracefully with custom options
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail when xcresult path does not exist, got '$OUTCOME'"
            exit 1
          fi
          echo "PASS: action correctly failed with custom options when xcresult bundle was not found"
