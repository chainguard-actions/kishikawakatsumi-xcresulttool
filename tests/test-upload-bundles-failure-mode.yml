name: Test upload-bundles failure mode input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create a minimal fake xcresult bundle directory
        run: |
          mkdir -p /tmp/fake3/Test.xcresult
          echo '{}' > /tmp/fake3/Test.xcresult/Info.plist

      - name: Run action with upload-bundles=failure (expect failure - xcrun not available on Linux)
        id: action
        uses: .
        with:
          path: /tmp/fake3/Test.xcresult
          token: ${{ github.token }}
          title: 'Test Results'
          show-passed-tests: 'true'
          show-code-coverage: 'true'
          upload-bundles: 'failure'
        continue-on-error: true

      - name: Assert action failed (xcrun not available on Linux)
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail (xcrun not available on Linux), but got outcome: $OUTCOME"
            exit 1
          fi
          echo "PASS: action correctly failed when xcrun is not available (upload-bundles=failure scenario)"
